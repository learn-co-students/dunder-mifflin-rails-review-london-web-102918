<!DOCTYPE html>
<html lang="" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>

    <h1><%= @employee.name %></h1>

    <img src="<%=@employee.img_url%>" alt="A photo of <%=@employee.name%>" style="width:400px;height:300px;">

      <p>Alias: <%= @employee.alias %></p>
      <p>Title: <%= @employee.title %></p>
      <p>Office: <%= @employee.office %></p>
      <p>Dog: <%= link_to @employee.dog.name, dog_path(@employee.dog) %></p>

  </body>
</html>
