<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <div>
    <h1>Name: <%= @dog.name %> </h1>
    <p>Breed: <%= @dog.breed %>  </p>
    <p>Age: <%= @dog.age %></p>
    <p>Employees: <% @dog.employees.each do |e| %>
      <li><%= e.to_s %></li>
      <% end %>
      </div>
  </body>
</html>
