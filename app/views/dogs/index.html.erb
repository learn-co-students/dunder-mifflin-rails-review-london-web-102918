<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Dog List</title>
  </head>
  <body>
    <h1>Dog List</h1>
    <ul>
    <% @dogs.each do |d| %>
    <li><%= link_to d.name, dog_path(d) %></li><br>
    <% end %>
    </ul>
    <%= link_to "sort by employees", dogs_url({:name => "sort" })%>
  </body>
</html>
